databaseChangeLog:
  - changeSet:
      id: init
      author: inventory
      preConditions:
        - onFail: MARK_RAN
          not:
            tableExists:
              tableName: inventory
      changes:
        - createTable:
            columns:
              - column:
                  autoIncrement: true
                  constraints:
                    nullable: false
                    primaryKey: true
                    primaryKeyName: inventory_pkey
                  name: id
                  type: BIGINT
              - column:
                  constraints:
                    nullable: false
                  name: name
                  type: varchar(255)
            tableName: inventory